apiVersion: v1
kind: Service
metadata:
  name: todo-backend
  namespace: crud-app
spec:
  selector:
    app: todo-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
